import { NonterminalKind } from '@nomicfoundation/slang/cst';
import { joinExisting } from '../slang-utils/join-existing.js';
import { extractVariant } from '../slang-utils/extract-variant.js';
import { SlangNode } from './SlangNode.js';
import { TypeName } from './TypeName.js';
import { TerminalNode } from './TerminalNode.js';
export class ErrorParameter extends SlangNode {
    constructor(ast, collected, options) {
        super(ast, collected);
        this.kind = NonterminalKind.ErrorParameter;
        this.typeName = extractVariant(new TypeName(ast.typeName, collected, options));
        if (ast.name) {
            this.name = new TerminalNode(ast.name, collected);
        }
        this.updateMetadata(this.typeName);
    }
    print(path, print) {
        return joinExisting(' ', [
            path.call(print, 'typeName'),
            path.call(print, 'name')
        ]);
    }
}
//# sourceMappingURL=ErrorParameter.js.map