import { NonterminalKind } from '@nomicfoundation/slang/cst';
import { extractVariant } from '../slang-utils/extract-variant.js';
import { SlangNode } from './SlangNode.js';
import { TypeName } from './TypeName.js';
import { Expression } from './Expression.js';
export class ArrayTypeName extends SlangNode {
    constructor(ast, collected, options) {
        super(ast, collected);
        this.kind = NonterminalKind.ArrayTypeName;
        this.operand = extractVariant(new TypeName(ast.operand, collected, options));
        if (ast.index) {
            this.index = extractVariant(new Expression(ast.index, collected, options));
        }
        this.updateMetadata(this.operand, this.index);
    }
    print(path, print) {
        return [path.call(print, 'operand'), '[', path.call(print, 'index'), ']'];
    }
}
//# sourceMappingURL=ArrayTypeName.js.map